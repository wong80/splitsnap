# splitsnap
Snap a receipt, split the bill. Django + HTMX app that extracts line items with a vision LLM, lets friends claim items via a share link, and calculates exactly who owes whom, down to the cent. Supports custom shares, discounts, and multiple payers.
